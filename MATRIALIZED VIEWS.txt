-- MATEIRALIZED VIEWS

CREATE MATERIALIZED VIEW MV_EMP
REFRESH COMPLETE
AS SELECT * FROM SCOTT.EMP;

SELECT * FROM MV_EMP
WHERE DEPTNO = 30;

ALTER MATERIALIZED VIEW MV_EMP
SET ENAME = 'CLAKE'
WHERE EMPNO = 7698;


DROP TABLE EMP;

CREATE TABLE EMP(
    ENAME VARCHAR2(20),
    MOB VARCHAR2(10),
    EMP_ID VARCHAR2(5)
    );

ALTER TABLE EMP ADD CONSTRAINTS EMP_PK PRIMARY KEY(EMP_ID);
ALTER TABLE EMP ADD CONSTRAINTS EMP_PK1 PRIMARY KEY(MOB); 

-- NOTE : ORA-02260: table can have only one primary key 




